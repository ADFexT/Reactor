项目过程说明
前两天：理解架构模式，弄清楚各个接口之间的关系，主reactor以及从reactor的实现逻辑
第三天：开始着手搭建项目，从头到尾敲代码完成基础功能搭建，其中安全线程仅支持存放文件描述符无法追踪客户端信息
第四天：优化安全线程队列以及哈希表值结构体存储信息


后续优化任务:
优先级 1：开发调试阶段优先做（改动小、收益高）
补充错误日志的 errno 信息
所有系统调用（epoll_ctl、accept、read、write）失败时，补充 strerror(errno) 打印，能极大提升调试效率，快速定位是权限、参数还是网络问题。
优化线程 ID 查找逻辑
用哈希表缓存 epollfd -> 线程ID 的映射，替代每次事件触发时的遍历查找，改动量极小，线程数越多收益越明显。
优先级 2：功能稳定后做（提升健壮性与优雅性）
Worker 线程独立 stopfd
替代当前 “往队列塞停止信号” 的方案，避免停止时截断队列中还未处理的有效连接，实现更优雅的退出；同时和主 Reactor 的停止机制保持设计一致。
TCP 保活与连接超时清理
为客户端 socket 开启 TCP Keepalive，应对半开连接、网络异常断开的场景；后续可再增加空闲连接超时清理，避免无效连接占用资源。
优先级 3：架构迭代时做（提升可维护性与扩展性）
封装 Server 顶层类，消除全局对象
把 receiver、processor、队列封装到一个 Server 类中，消除全局指针，降低模块耦合，后续新增配置、业务逻辑时更方便扩展。
事件分发策略优化
当前连接是按顺序轮询分发到 Worker，后续可改为按 Worker 负载（连接数）分发，进一步均衡多线程负载。
